<!DOCTYPE html>
<html>
    <head>
        <title>Users index page</title>
    </head>
    <body>
        <h1>Users</h1>
        <table>
            <tr>
                <th>FirstName</th>
                <th>LastName</th>
                <th>Age</th>
                <th>Email</th>
            </tr>
            <% @testerb.each do |item| %>
                <tr>
                    <td><%= item["firstname"] %></td>
                    <td><%= item["lastname"] %></td>
                    <td><%= item["age"] %></td>
                    <td><%= item["email"] %></td>
                </tr>
            <% end %>
        </table>
    </body>
</html>